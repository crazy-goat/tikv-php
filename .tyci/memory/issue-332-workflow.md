Issue #332 (TEST-12) done: RawKvScanner::validateScanLimit() now rejects negative limits with InvalidArgumentException('Scan limit must be 0 or greater') before the ===0 check, so a negative limit can no longer reach the uint32 RawScanRequest::setLimit() and wrap to ~4B unbounded scan. Message is byte-identical to TxnReader::normalizeScanLimit(). PR #466 squash-merged to master (9addc13). Tests cover every scan entry point (scan/reverseScan/scanPrefix/batchScan/scanIterator/scanPrefixIterator on RawKvScanner+RawKvClient, Transaction::scan), boundary values 1/MAX(10240)/MAX+1, and cross-path message agreement. Local: 714 unit tests pass, cs/rector/phpstan clean; CI all green (lint, unit 8.2/8.3/8.4, grpc, E2E). Out-of-scope finding (stale-region-capture in RawKvRangeOps deleteRange/checksum) verified real but already tracked as open issues #190 (RAW-05) and #235 (REG-04) — do NOT file a duplicate.
